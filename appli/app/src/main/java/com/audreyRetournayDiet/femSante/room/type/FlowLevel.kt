package com.audreyRetournayDiet.femSante.room.type

/**
 * Abondance du flux menstruel, telle que saisie par l'utilisatrice.
 * Information cliniquement utile pour le suivi de l'endométriose.
 */
enum class FlowLevel {
    /** Flux léger. */
    LEGER,

    /** Flux moyen. */
    MOYEN,

    /** Flux abondant. */
    ABONDANT
}
